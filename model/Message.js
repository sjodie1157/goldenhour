import { connection as db } from '../config/index.js';

class Message {
    getMessages(req, res){
        // get messages from user
        // only if appropriate
    }
    sendMessage(req, res){}
}

export {
    Message
}